# Level Me — 配布用プロトタイプ

5人にURLを渡して試してもらうための最小構成。

## 中身

    index.html
    src/main.jsx
    src/App.jsx      ← アプリ本体（画面もAIへの指示文もすべてここ）
    api/chat.js      ← AIを呼ぶサーバー関数。APIキーはここでしか使わない

## 手元で動かす

    npm install
    npm run dev

`.env.local` を作って自分のキーを入れる（このファイルはGitに上げない）：

    ANTHROPIC_API_KEY=sk-ant-...

キーは https://console.anthropic.com で発行する。

## URLで配る（Vercel・無料枠）

1. このフォルダをGitHubにpushする
2. vercel.com にGitHubアカウントでログイン → New Project → このリポジトリを選ぶ
3. Environment Variables に `ANTHROPIC_API_KEY` を登録する
4. Deploy を押す → `https://〇〇.vercel.app` が発行される

そのURLを配れば、スマホのブラウザでそのまま動く。

## 配る前に必ず確認すること

- **APIキーを `src/` の中に書かない。** ブラウザから丸見えになり、他人に使われて課金される。
- Vercelの Usage で使用量アラートを設定しておく。
- テスターには「記録はブラウザを閉じると消える」と伝える。保存機能はまだ無い。

## 入っているもの

- キャラメイク（名前・かみがた・かみの色・はだの色・ふくの色）
- 3つの質問 → AIがロードマップを作る
- 使い方の説明4枚（あとから「?」でいつでも読み直せる）
- ジャーナリング（AIが3回問いかける）→ 気づきと次の一歩
- マイコンパス（山道・価値観の集計・まとめレポート）
- 見つける（記録から活動の方向を提案）

## いまの制限（意図的にそうしている）

- データを保存していない。リロードで消える。キャラも作り直しになる。
- ログインが無い。
- 「見つける」は活動のジャンルとキーワードまでしか出さない。実在の団体名をAIに作らせないため。

## 次に足すとしたら

1. 保存（Supabase等）とログイン
2. 週次のふり返り通知
3. Expo（React Native）へ移植してストア配信
